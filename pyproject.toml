[project]
name = "math-simulator"
version = "0.1.0"
description = "Math simulator for kids"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flet[all]>=0.80.2",
]

[tool.flet]
app_name = "Math Simulator"
org = "ua.mathsimulator"
assets_dir = "assets"
